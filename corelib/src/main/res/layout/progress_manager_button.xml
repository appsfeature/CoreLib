<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content" >

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@color/colorTransparent"
        android:layout_centerHorizontal="true"
        android:padding="6dp"
        android:indeterminateDrawable="@drawable/progress_custom_white" />


    <Button
        android:id="@+id/btnRetry"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/colorTransparent"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:textAllCaps="false"
        android:textSize="@dimen/progressManagerTextSizeRetryButton"
        android:textColor="@color/progressTextColorButton"
        android:text="@string/progress_manager_retry"  />


</RelativeLayout>